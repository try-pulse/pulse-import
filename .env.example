# Copy to .env for local convenience (optional — env vars also work).
# Never commit a real .env; it is gitignored.

PULSE_ACCESS_TOKEN=
# PULSE_API_KEY=          # alias for the same JWT
PULSE_API_URL=https://api.trypulse.tech/api/v1
PULSE_WORKSPACE_ID=
